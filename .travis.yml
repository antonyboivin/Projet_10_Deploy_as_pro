language: python
python:
  - '3.6'

before_script:
  - pip install -r ./PurBeurre/requirements.txt

branches:
  only:
    - staging  

env: DJANGO_SETTINGS_MODULE="purbeurre.settings"

services:
  - postgresql

script:
  - python ./home/travis/build/antonyboivin/Projet_10_Deploy_as_pro/PurBeurre/manage.py test
